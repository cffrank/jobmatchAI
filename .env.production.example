# =============================================================================
# JobMatch AI Frontend - Production Environment Variables
# =============================================================================
# This file shows the environment variables needed for frontend deployment.
# Set these in Railway Dashboard > Project > Variables tab
# NEVER commit actual .env files to version control.

# -----------------------------------------------------------------------------
# Supabase Configuration (Frontend)
# -----------------------------------------------------------------------------
# Get these from Supabase Dashboard > Project Settings > API
# https://app.supabase.com/project/lrzhpnsykasqrousgmdh/settings/api

# Supabase Project URL (PUBLIC - safe for browser)
VITE_SUPABASE_URL=https://lrzhpnsykasqrousgmdh.supabase.co

# Supabase Anonymous/Public Key (PUBLIC - safe for browser)
# This key respects Row Level Security (RLS) policies
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxyemhwbnN5a2FzcXJvdXNnbWRoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MzUxMDIxMzQsImV4cCI6MjA1MDY3ODEzNH0.your-actual-anon-key

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# URL to the deployed backend API on Railway
# Format: https://your-backend-service.railway.app
# Get this AFTER deploying the backend service
VITE_BACKEND_URL=https://backend-production-xxxx.up.railway.app

# -----------------------------------------------------------------------------
# LinkedIn OAuth Configuration
# -----------------------------------------------------------------------------
# LinkedIn OAuth Client ID
# Get this from https://www.linkedin.com/developers/apps
VITE_LINKEDIN_CLIENT_ID=your-linkedin-client-id

# LinkedIn OAuth Redirect URI
# This should point to your FRONTEND URL (Railway will provide this)
# Format: https://your-frontend-service.railway.app/auth/callback/linkedin
VITE_LINKEDIN_REDIRECT_URI=https://frontend-production-xxxx.up.railway.app/auth/callback/linkedin

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Application name (optional)
VITE_APP_NAME=JobMatch AI

# Environment (optional - defaults to production)
VITE_ENV=production

# -----------------------------------------------------------------------------
# IMPORTANT NOTES
# -----------------------------------------------------------------------------
# 1. ALL variables with VITE_ prefix are PUBLIC and visible in browser
# 2. NEVER put secrets (API keys, service role keys) in frontend variables
# 3. Supabase ANON key is safe because it's protected by RLS policies
# 4. Update VITE_BACKEND_URL after deploying backend to Railway
# 5. Update VITE_LINKEDIN_REDIRECT_URI after deploying frontend to Railway
# 6. After deployment, update LinkedIn OAuth redirect URIs in LinkedIn Developer Console
# -----------------------------------------------------------------------------
