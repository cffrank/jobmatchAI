# =============================================================================
# JobMatch AI Frontend - Cloudflare Pages Environment Variables
# =============================================================================
# Set these in Cloudflare Dashboard > Pages > Settings > Environment Variables
# This file is a TEMPLATE - do not commit with real values
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Frontend)
# -----------------------------------------------------------------------------
# Get these from Supabase Dashboard > Project Settings > API

# Supabase Project URL (PUBLIC - safe for browser)
VITE_SUPABASE_URL=https://wpupbucinufbaiphwogc.supabase.co

# Supabase Anonymous/Public Key (PUBLIC - safe for browser)
# Using the new publishable key format
VITE_SUPABASE_ANON_KEY=sb_publishable_h6erYLL-Ye6oD7pNyWLGBw_GYbxQ4OA

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# URL to the deployed backend API on Cloudflare Workers
# Format: https://your-worker-name.your-account.workers.dev
# Or custom domain: https://api.your-domain.com
VITE_BACKEND_URL=https://jobmatch-api.your-account.workers.dev

# -----------------------------------------------------------------------------
# LinkedIn OAuth Configuration (Optional)
# -----------------------------------------------------------------------------
# LinkedIn OAuth Client ID
# Get this from https://www.linkedin.com/developers/apps
VITE_LINKEDIN_CLIENT_ID=your-linkedin-client-id

# LinkedIn OAuth Redirect URI
# This should point to your Cloudflare Pages frontend URL
# Format: https://your-project.pages.dev/auth/callback/linkedin
# Or custom domain: https://app.your-domain.com/auth/callback/linkedin
VITE_LINKEDIN_REDIRECT_URI=https://jobmatch-ai.pages.dev/auth/callback/linkedin

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
VITE_APP_NAME=JobMatch AI
VITE_ENV=production

# -----------------------------------------------------------------------------
# IMPORTANT NOTES
# -----------------------------------------------------------------------------
# 1. ALL variables with VITE_ prefix are PUBLIC and visible in browser
# 2. NEVER put secrets (API keys, service role keys) in frontend variables
# 3. Supabase ANON key is safe because it's protected by RLS policies
# 4. Set these in Cloudflare Pages Dashboard BEFORE deploying
# 5. After deployment, update LinkedIn OAuth redirect URIs in LinkedIn Console
# 6. For preview deployments, you may need different values
# -----------------------------------------------------------------------------
