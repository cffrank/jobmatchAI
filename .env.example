# JobMatch AI Environment Variables
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git

# ============================================
# Supabase Configuration (Frontend)
# ============================================
# Get these values from:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Supabase Project URL (PUBLIC)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon/Public Key (PUBLIC)
# This key is safe to use in the browser
# It respects Row Level Security (RLS) policies
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================
# Backend API Configuration (Optional)
# ============================================
# URL to the backend API server
# Local development: http://localhost:3000
# Production: Your deployed backend URL
VITE_BACKEND_URL=http://localhost:3000

# ============================================
# Notes
# ============================================
# 1. VITE_ prefix makes variables available in the frontend
# 2. Frontend variables are PUBLIC - anyone can see them
# 3. Never put backend secrets (API keys, etc.) in this file
# 4. Backend secrets should be set in Supabase Edge Functions or your backend
# 5. The anon key is protected by Row Level Security (RLS) policies
# 6. See Supabase documentation for more on security best practices
